#!/usr/bin/env ruby
# frozen_string_literal: true

require 'scryfall'

puts 'Enter search'
search = gets.chomp
puts 'Enter page'
page = gets.chomp

cards = Scryfall::Cards.search(search, page)
puts "Here are the cards: #{cards}"
